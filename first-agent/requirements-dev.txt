# Development dependencies — not required in production
#
# Install:  pip install -r requirements-dev.txt

# Pre-commit framework
pre-commit>=3.7.0

# Python formatter (also run by pre-commit, listed here for IDE / CI use)
black>=24.10.0

# Python linter (also run by pre-commit, listed here for IDE / CI use)
flake8>=7.1.1

# AWS SDK — required by ai-review hook and bedrock_hello.py
boto3>=1.34.0
